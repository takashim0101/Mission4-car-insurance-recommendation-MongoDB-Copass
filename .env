# C:\Level 5\Mission4_MongoDB_Integration\.env
# This file contains your actual environment variables.
# DO NOT commit this file to Git or share it publicly!

GOOGLE_API_KEY=AIzaSyAT5jcmehdJR9ql9aJH87Oj2fsg_WdnSok

# MongoDB authentication (RECOMMENDED for security)
MONGO_INITDB_ROOT_USERNAME=your_mongo_user # Choose a username (e.g., myuser)
MONGO_INITDB_ROOT_PASSWORD=your_mongo_password # Choose a strong password






# MongoDB authentication (RECOMMENDED for security)
# These variables will be used by the MongoDB container to set up a root user.
# Uncommented: Ensure these lines DO NOT start with a '#' in your actual .env file.
MONGO_INITDB_ROOT_USERNAME=[YOUR_MONGO_USERNAME] # <-- IMPORTANT: Choose a username (e.g., myappuser, admin_user)
MONGO_INITDB_ROOT_PASSWORD=[YOUR_MONGO_PASSWORD] # <-- IMPORTANT: Choose a strong, unique password

# MONGO_URI is not typically needed in .env for Docker Compose setup,
# as services communicate via their service names (e.g., 'mongodb') within the Docker network.
# MONGO_URI="mongodb://localhost:27017/mydatabase"